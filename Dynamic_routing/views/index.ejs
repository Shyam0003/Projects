<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="bg-zinc-900">
        <h1 class="text-red-600">This is EJS File: <%= title %>
        </h1>
        <p class="text-white">
            <%= message %>
        </p>
    </div>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form act ion="/create" method="post">
                <input type="text" placeholder="Title goes here" name="title"
                    class="block w-full px-5 py-2 bg-zinc-800 rounded-md">
                <textarea name="details" cols="30" rows="3"
                    class="block w-full rounded-md mt-2 px-5 py-2 bg-zinc-800 resize-none"
                    placeholder="Write your task details here..."></textarea>
                <input type="submit" value="Create Task" class="mt-2 bg-blue-600 px-5 py-2 rounded-md">
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap p-10">
            <% if (files && files.length> 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-2xl tracking-tighter">
                            <%= val %>
                        </h1>
                        <div class="flex justify-between items-center w-full">
                            <a href="/file/<%= val %>" class="text-blue-500 inline-block ">Read More</a>
                            <a href="/edit/<%= val%>" class="text-zinc-600">edit</a>
                        </div>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <h3 class="text-zinc-600">No tasks available</h3>
                            <% } %>
        </div>
    </div>
</body>

</html>